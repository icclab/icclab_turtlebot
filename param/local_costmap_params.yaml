local_costmap:
  global_frame: /odom
  robot_base_frame: /base_footprint

  update_frequency: 1.0 #both used to be 5 but map update loop missed was happening
  publish_frequency: 1.0
  transform_tolerance: 0.5  

  static_map: false  
  rolling_window: true
  width: 4.0
  height: 4.0
  resolution: 0.02

